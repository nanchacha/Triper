{
  "name": "triper",
  "version": "1.0.0",
  "description": "Google Maps web app",
  "scripts": {
    "build": "mkdir -p public && cp index.html style.css script.js public/ && echo \"window.GOOGLE_MAPS_API_KEY = '${GOOGLE_MAPS_API_KEY}';\" > public/config.js"
  }
}
